{
    "getServiceByName": "SELECT service_name, service_cost FROM services WHERE service_name LIKE $1;",
    "getUsersNames": "SELECT user_name FROM users;",
    "getUserId": "SELECT user_id FROM users WHERE user_name = $1",
    "loggedUser": "SELECT user_phone, user_email WHERE user_name = $1",
    "createInvoice": "INSERT INTO invoices (invoice_services, invoice_amount, invoice_status, user_id) VALUES ($1, $2, $3, $4)"
}